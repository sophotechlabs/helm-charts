exclude_path = [
  "CHANGELOG.md",
  "charts/keycloak/CHANGELOG.md",
  "charts/keycloak-config/CHANGELOG.md",
]

exclude = [
  "^http://127\\.0\\.0\\.1",
  "^http://localhost",
  "^https://img\\.shields\\.io",
  # Published only once the first release runs.
  "^https://github\\.com/sophotechlabs/helm-charts/tree/main/charts/",
]

accept = [200, 206, 403, 429]
max_retries = 2
timeout = 20
no_progress = true
