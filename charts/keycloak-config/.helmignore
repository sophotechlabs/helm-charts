.DS_Store
.git/
.gitignore
*.orig
*.tmp
*.bak
*.swp
# Anchored: an unanchored tests/ also matches templates/tests/, which would
# quietly drop the helm test hooks out of the packaged chart.
/tests/
/ci/
/__snapshot__/
